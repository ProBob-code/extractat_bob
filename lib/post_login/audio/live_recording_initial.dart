import 'package:flutter/material.dart';

class LiveRecordingInitial extends StatelessWidget {
  const LiveRecordingInitial({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Live Recording Initial")),
      body: const Center(child: Text("This is the Live Recording Initial Page")),
    );
  }
}
