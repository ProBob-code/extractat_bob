import 'package:flutter/material.dart';

class AudioDatasets extends StatelessWidget {
  const AudioDatasets({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Audio Datasets")),
      body: const Center(child: Text("This is the Audio dataset Page")),
    );
  }
}
