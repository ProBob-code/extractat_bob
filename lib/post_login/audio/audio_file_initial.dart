import 'package:flutter/material.dart';

class AudioFileInitial extends StatelessWidget {
  const AudioFileInitial({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Audio File Initial")),
      body: const Center(child: Text("This is the Audio File Initial Page")),
    );
  }
}
