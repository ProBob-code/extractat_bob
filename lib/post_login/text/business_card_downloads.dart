import 'package:flutter/material.dart';

class BusinessCardDownloads extends StatelessWidget {
  const BusinessCardDownloads({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Business Card Downloads")),
      body: const Center(child: Text("This is the Business Card download Page")),
    );
  }
}
