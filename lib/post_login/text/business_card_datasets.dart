import 'package:flutter/material.dart';

class BusinessCardDatasets extends StatelessWidget {
  const BusinessCardDatasets({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Business Card Datasets")),
      body: const Center(child: Text("This is the Business Card dataset Page")),
    );
  }
}
