import 'package:flutter/material.dart';

class PDFInitial extends StatelessWidget {
  const PDFInitial({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Portable Document Format Initial")),
      body: const Center(child: Text("This is the PDF Initial Page")),
    );
  }
}
